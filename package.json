{
  "name": "ewa-protocol",
  "version": "1.0.0",
  "description": "Privacy-preserving Earned Wage Access on Monad × Unlink",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.ts",
    "deploy:monad": "hardhat run scripts/deploy.ts --network monad",
    "attest": "hardhat run scripts/issue-attestation.ts --network monad",
    "payroll": "hardhat run scripts/simulate-payroll.ts --network monad"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.0"
  }
}
